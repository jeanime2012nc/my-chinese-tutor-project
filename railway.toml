[build]
builder = "nixpacks"
buildCommand = "pnpm install && npx taro build --type h5 && pnpm --filter server build"

[deploy]
startCommand = "cd server && node dist/main.js"
healthcheckPath = "/api/health"
restartPolicyType = "always"